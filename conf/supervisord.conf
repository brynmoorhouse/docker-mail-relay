[supervisord]
nodaemon = true              ; Stay in foreground so Docker sees PID 1
user = root
logfile = /dev/stdout        ; Log to container stdout
logfile_maxbytes = 0         ; Disable log rotation
pidfile = /var/run/supervisord.pid
loglevel = info

[include]
files = /etc/supervisor.d/*.ini
